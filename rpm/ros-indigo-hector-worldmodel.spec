Name:           ros-indigo-hector-worldmodel
Version:        0.3.2
Release:        0%{?dist}
Summary:        ROS hector_worldmodel package

Group:          Development/Libraries
License:        BSD
URL:            http://ros.org/wiki/hector_worldmodel
Source0:        %{name}-%{version}.tar.gz

BuildArch:      noarch

Requires:       ros-indigo-hector-object-tracker
Requires:       ros-indigo-hector-worldmodel-geotiff-plugins
Requires:       ros-indigo-hector-worldmodel-msgs
BuildRequires:  ros-indigo-catkin

%description
The hector_worldmodel stack helps to collect and fuse information about objects
in the world. It is used by Team Hector Darmstadt to localize victims and QR
codes in the Robocup Rescue scenario. See hector_object_tracker for further
details.

%prep
%setup -q

%build
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/indigo/setup.sh" ]; then . "/opt/ros/indigo/setup.sh"; fi
mkdir -p obj-%{_target_platform} && cd obj-%{_target_platform}
%cmake .. \
        -UINCLUDE_INSTALL_DIR \
        -ULIB_INSTALL_DIR \
        -USYSCONF_INSTALL_DIR \
        -USHARE_INSTALL_PREFIX \
        -ULIB_SUFFIX \
        -DCMAKE_INSTALL_PREFIX="/opt/ros/indigo" \
        -DCMAKE_PREFIX_PATH="/opt/ros/indigo" \
        -DSETUPTOOLS_DEB_LAYOUT=OFF \
        -DCATKIN_BUILD_BINARY_PACKAGE="1" \

make %{?_smp_mflags}

%install
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/indigo/setup.sh" ]; then . "/opt/ros/indigo/setup.sh"; fi
cd obj-%{_target_platform}
make %{?_smp_mflags} install DESTDIR=%{buildroot}

%files
/opt/ros/indigo

%changelog
* Sun Dec 21 2014 Johannes Meyer <meyer@fsr.tu-darmstadt.de> - 0.3.2-0
- Autogenerated by Bloom

